// Admin log in
// Admin can edit perfume names and prices
// bank transfers with a code to use to track record of sales. ready to checkout, use code "machalla as desc"

// USERS
// okay login,
// have history list
// profile picture and name showing
// have made for you,"because you liked ${USERS_LAST_PURCHASE} try ${NEWER_UPLOADS}. lock it up at ${NEWER_UPLOADS_PRICE} "


// STACK 
// mongodb for database
// 


const http = require("http");
const fs = require("fs");
const path = require("path");